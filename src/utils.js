exports.studentBlock = (std) => {
  if (std === "grad") return "Graduate";

  if (std === "be") return "Backend";

  if (std === "proj") return "Project";
  if (std === "fun") return "Fundamental";

  if (std === "fe") return "Frontend";
};
